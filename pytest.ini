[pytest]
testpaths = tests
addopts = -q
pythonpath = .
markers =
    manual: интеграционный прогон реального процесса адаптера (спавн backend-adapter.py с fake-бэкендом; идёт и в общем прогоне, ~30 с; точечно — pytest -m manual)
